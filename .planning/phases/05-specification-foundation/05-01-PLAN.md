---
phase: 05-specification-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "src/domain/entities/SpecificationTemplate.ts",
  "src/services/SpecificationService.ts",
  "src/adapters/ISpecificationService.ts",
  "src/infrastructure/ioc/container.ts"
]
autonomous: true

must_haves:
  truths:
    - "System can validate 6-section specification structure"
    - "SpecificationService can create and update specifications"
    - "Service integrates with existing IoC container patterns"
  artifacts:
    - path: "src/domain/entities/SpecificationTemplate.ts"
      provides: "6-section schema with Zod validation"
      exports: ["SpecificationTemplate", "SpecificationTemplateSchema", "SpecificationSection"]
    - path: "src/services/SpecificationService.ts"
      provides: "Business logic for specification CRUD"
      exports: ["SpecificationService"]
    - path: "src/adapters/ISpecificationService.ts"
      provides: "Service interface for IoC"
      exports: ["ISpecificationService"]
  key_links:
    - from: "src/services/SpecificationService.ts"
      to: "src/domain/entities/SpecificationTemplate.ts"
      via: "import and validation"
      pattern: "SpecificationTemplateSchema\\.(parse|safeParse)"
    - from: "src/infrastructure/ioc/container.ts"
      to: "src/services/SpecificationService.ts"
      via: "IoC binding"
      pattern: "bind.*ISpecificationService"
---

<objective>
Create specification foundation with 6-section template schema and service layer extensions

Purpose: Enable structured specifications within work items using existing JSONB storage and IoC patterns
Output: Domain types, validation schemas, and business service ready for UI integration
</objective>

<execution_context>
@/Users/briannielsen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/briannielsen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/briannielsen/forge/.planning/PROJECT.md
@/Users/briannielsen/forge/.planning/ROADMAP.md
@/Users/briannielsen/forge/.planning/STATE.md
@/Users/briannielsen/forge/.planning/phases/05-specification-foundation/05-RESEARCH.md

# Existing architecture patterns
@/Users/briannielsen/forge/src/domain/entities/WorkItem.ts
@/Users/briannielsen/forge/src/services/WorkItemService.ts
@/Users/briannielsen/forge/src/infrastructure/ioc/container.ts
</context>

<tasks>

<task type="auto">
  <name>Create SpecificationTemplate Schema</name>
  <files>src/domain/entities/SpecificationTemplate.ts</files>
  <action>
Create SpecificationTemplate.ts with 6-section structure following research recommendations:

1. Define SpecificationSection interface with content, status, lastUpdated, wordCount
2. Define SpecificationTemplate interface with 6 sections: requirements, design, frontend, backend, integration, test
3. Create Zod schemas for runtime validation following existing class-validator patterns
4. Add templateVersion field defaulting to "1.0" for future schema evolution
5. Include helper methods for completion percentage calculation and section validation
6. Use consistent status enum: 'empty' | 'draft' | 'review' | 'complete'

Follow existing domain entity patterns from WorkItem.ts - use class-validator decorators, include validation methods, provide JSON serialization.
  </action>
  <verify>npm run typecheck passes, SpecificationTemplateSchema.parse({}) creates valid default template</verify>
  <done>SpecificationTemplate domain entity exists with 6-section validation and follows project patterns</done>
</task>

<task type="auto">
  <name>Create SpecificationService</name>
  <files>src/services/SpecificationService.ts, src/adapters/ISpecificationService.ts</files>
  <action>
Create SpecificationService following existing WorkItemService patterns:

1. Create ISpecificationService interface with methods: updateSpecification, getSpecification, validateCompleteness
2. Implement SpecificationService as @injectable class with constructor injection of IWorkItemRepository and AuditTrailService
3. Add updateSpecification method that updates WorkItem.spec JSONB field using existing updateSpec method
4. Add getSpecification method that extracts/validates spec from WorkItem using SpecificationTemplateSchema
5. Add validateCompleteness method that checks each section status and returns completion report
6. Include audit trail logging for specification changes following existing patterns
7. Handle validation errors and provide meaningful error messages

Use existing service patterns - inject dependencies via IoC, emit audit events, handle errors gracefully. Do NOT add new repository - reuse existing IWorkItemRepository.
  </action>
  <verify>SpecificationService compiles without errors and follows dependency injection patterns</verify>
  <done>SpecificationService provides CRUD operations for WorkItem specifications with validation and audit logging</done>
</task>

<task type="auto">
  <name>Register Service in IoC Container</name>
  <files>src/infrastructure/ioc/container.ts</files>
  <action>
Register SpecificationService in the IoC container following existing patterns:

1. Import ISpecificationService and SpecificationService
2. Add container binding: bind<ISpecificationService>('ISpecificationService').to(SpecificationService)
3. Follow existing registration patterns used for WorkItemService, AuditTrailService
4. Ensure the binding comes after WorkItemRepository registration (dependency requirement)
5. Maintain alphabetical ordering of service registrations for consistency

Follow exact pattern from existing services - use same binding syntax and container configuration approach.
  </action>
  <verify>Container builds without errors and can resolve ISpecificationService</verify>
  <done>SpecificationService is registered in IoC container and available for dependency injection</done>
</task>

</tasks>

<verification>
- npm run typecheck passes for all created files
- SpecificationTemplateSchema can parse empty object to create default template
- Container can resolve ISpecificationService without circular dependencies
- All files follow existing project patterns and conventions
</verification>

<success_criteria>
1. SpecificationTemplate domain entity exists with 6-section structure (Requirements, Design, Frontend, Backend, Integration, Test)
2. SpecificationService provides business logic for specification CRUD operations
3. Service integrates with existing IoC container and follows established patterns
4. All TypeScript compilation passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-specification-foundation/05-01-SUMMARY.md`
</output>