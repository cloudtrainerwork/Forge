---
phase: 06-export-engine
plan: 03
type: execute
wave: 5
depends_on: [06-02b]
files_modified: [
  "src/api/routes/exports.ts",
  "src/api/server.ts"
]
autonomous: false
requirements: [EXPORT-01, EXPORT-05]
user_setup: []

must_haves:
  truths:
    - "User can export work item specifications via REST API endpoint"
    - "API returns GSD XML file as downloadable response with proper headers"
    - "Export endpoint integrates with existing server routing patterns"
  artifacts:
    - path: "src/api/routes/exports.ts"
      provides: "REST endpoints for export operations"
      exports: ["exportsRoutes"]
    - path: "src/api/server.ts"
      provides: "Export routes integration"
      contains: "/api/exports route registration"
  key_links:
    - from: "src/api/routes/exports.ts"
      to: "src/services/ExportService.ts"
      via: "service factory dependency injection"
      pattern: "serviceFactory\\.getExportService"
    - from: "src/api/server.ts"
      to: "src/api/routes/exports.ts"
      via: "Express router registration"
      pattern: "app\\.use.*exports"
---

<objective>
Create REST API endpoints for GSD XML export with file download capabilities

Purpose: Enable users to export work item specifications as GSD XML files through REST API with proper file download headers
Output: Complete API integration ready for frontend consumption with downloadable file responses
</objective>

<execution_context>
@/Users/briannielsen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/briannielsen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-export-engine/06-RESEARCH.md

# Plan Dependencies
@.planning/phases/06-export-engine/06-02-SUMMARY.md

# Existing API Patterns
@src/api/routes/specifications.ts
@src/api/server.ts
@src/factories/ServiceFactory.ts

# Service Dependencies
@src/services/ExportService.ts
@src/adapters/IExportService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create exports API routes</name>
  <files>src/api/routes/exports.ts</files>
  <action>
    Create REST API endpoints for export operations following established routing patterns:
    - POST /api/exports/gsd/:workItemId endpoint for GSD XML export
    - Use ServiceFactory pattern to get ExportService instance
    - Implement proper error handling with 400, 404, 500 status codes
    - Return file download response with proper headers (Content-Type: application/xml, Content-Disposition: attachment)
    - Include request validation for workItemId parameter
    - Add response timing headers for performance monitoring
    - Follow exact error handling patterns from specifications.ts routes
    - Include proper TypeScript types for request/response objects
    - Add JSDoc documentation for all endpoints

    Match routing patterns and error handling from existing specifications.ts and workItems.ts routes.
  </action>
  <verify>
    <automated>npm test src/api/routes/exports.test.ts -t "exports GSD XML endpoint"</automated>
  </verify>
  <done>Export API routes exist with proper file download responses and error handling</done>
</task>

<task type="auto">
  <name>Task 2: Integrate export routes into server</name>
  <files>src/api/server.ts</files>
  <action>
    Register export routes in main server following established patterns:
    - Import exportsRoutes function from routes/exports.ts
    - Register /api/exports route with app.use() following existing route patterns
    - Maintain alphabetical ordering of route registrations
    - Follow exact integration pattern used for specifications and workItems routes
    - Ensure proper middleware ordering (routes after middleware, before error handlers)
    - Add route registration in same location as other API routes

    Match the exact integration pattern used for specifications routes in server.ts.
  </action>
  <verify>
    <automated>npm run build && node -e "console.log('Server compiles successfully')"</automated>
  </verify>
  <done>Export routes integrated into server with proper route registration and ordering</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete GSD XML export system with API endpoints and file download capabilities</what-built>
  <how-to-verify>
    1. Start the backend server: `npm run dev` in project root
    2. Test the export endpoint:
       - POST to http://localhost:3001/api/exports/gsd/[existing-work-item-id]
       - Should return XML file download with proper headers
    3. Verify file content:
       - Downloaded file should be valid XML with GSD namespace
       - Should contain wave-based task structure
       - Should have maximum 3 tasks derived from specification sections
    4. Test error cases:
       - POST to http://localhost:3001/api/exports/gsd/nonexistent-id should return 404
       - Verify proper error messages and status codes
  </how-to-verify>
  <resume-signal>Type "approved" if export functionality works correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- Export API endpoints return proper file download responses with XML content
- Server integration follows established routing patterns
- File download headers properly configured for GSD XML files
- Error handling matches existing API error response patterns
- Export functionality demonstrates complete workflow from specification to GSD XML
</verification>

<success_criteria>
- POST /api/exports/gsd/:workItemId endpoint exists and returns downloadable XML files
- API responses include proper Content-Type and Content-Disposition headers
- Server routing integrates export endpoints following established patterns
- Export operations complete with proper error handling for missing work items
- Generated GSD XML files are valid and contain wave-based task structures
</success_criteria>

<output>
After completion, create `.planning/phases/06-export-engine/06-03-SUMMARY.md`
</output>