---
phase: 06-export-engine
plan: 05
type: execute
wave: 2
depends_on: [06-04]
files_modified: [src/api/routes/analytics.ts, src/api/routes/reports.ts, src/services/AnalyticsService.ts, src/services/ReportingService.ts, src/services/ReadinessService.ts]
autonomous: true
gap_closure: true
requirements: [EXPORT-01, EXPORT-02, EXPORT-05]

must_haves:
  truths:
    - "Analytics and reports routes compile without missing method errors"
    - "Service layer method calls match available interface implementations"
    - "Server can start successfully with all routes registered including exports"
    - "Export functionality remains unblocked by unrelated service compilation issues"
  artifacts:
    - path: "src/api/routes/analytics.ts"
      provides: "Corrected service method calls matching ServiceFactory interface"
      min_lines: 100
    - path: "src/api/routes/reports.ts"
      provides: "Corrected service method calls matching ServiceFactory interface"
      min_lines: 100
    - path: "src/services/AnalyticsService.ts"
      provides: "Fixed method signatures and type compatibility"
      min_lines: 500
  key_links:
    - from: "src/api/server.ts"
      to: "src/api/routes/analytics.ts"
      via: "Route registration in server startup"
      pattern: "analyticsRoutes"
    - from: "src/api/server.ts"
      to: "src/api/routes/reports.ts"
      via: "Route registration in server startup"
      pattern: "reportsRoutes"
    - from: "src/api/routes/exports.ts"
      to: "src/services/ExportService.ts"
      via: "Export functionality dependency on clean server compilation"
      pattern: "getExportService"
---

<objective>
Fix service layer compilation errors blocking export engine server functionality

Purpose: Resolve analytics and reports service method mismatches preventing server startup
Output: Clean server compilation enabling export API endpoint availability
</objective>

<execution_context>
@/Users/briannielsen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/briannielsen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-export-engine/06-VERIFICATION.md
@.planning/phases/06-export-engine/06-04-PLAN.md

# Phase 6 verification gaps requiring service layer fixes:
# Gap 3: Export endpoint integration blocked by server compilation errors
# Gap 4: Service follows patterns blocked by decorator configuration (fixed in 06-04)

# TypeScript compilation errors indicate missing/misnamed methods in:
# - ServiceFactory: getWorkItemRepository, getSprintService, getGroupingService
# - SprintService: getSprintById, getAllSprints methods
# - GroupingService: getGroupById, getAllGroups, getGroupForWorkItem methods
# - WorkItem: specification property missing
# - AuditTrailService: missing event types

<interfaces>
<!-- Current ServiceFactory interface that routes are trying to use -->

From src/factories/ServiceFactory.ts available methods:
```typescript
export class ServiceFactory {
  getExportService(): IExportService
  // Missing: getWorkItemRepository, getSprintService, getGroupingService
}
```

Analytics/Reports routes expect but ServiceFactory lacks:
```typescript
// Expected but missing:
serviceFactory.getWorkItemRepository()
serviceFactory.getSprintService()
serviceFactory.getGroupingService()
```

Common service method patterns in codebase:
```typescript
// Sprint services typically have:
getSprints(): Promise<Sprint[]>
// Not: getSprintById, getAllSprints

// Grouping services typically have:
getGroups(): Promise<Group[]>
// Not: getGroupById, getAllGroups, getGroupForWorkItem
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Fix analytics route service method calls</name>
  <files>src/api/routes/analytics.ts</files>
  <action>
    Fix the missing service method calls in analytics routes to match actual ServiceFactory interface.

    Current errors (lines 116, 118, 119):
    - `serviceFactory.getWorkItemRepository()` - method doesn't exist
    - `serviceFactory.getSprintService()` - method doesn't exist
    - `serviceFactory.getGroupingService()` - method doesn't exist

    Replace these method calls with available alternatives:
    1. Check ServiceFactory.ts for actual available methods
    2. If missing services are needed, either:
       - Comment out the problematic routes with TODO comments
       - Replace with placeholder responses that don't break compilation
       - Use alternative service access patterns

    Priority is compilation success - analytics functionality is not blocking export engine.
    Use pattern: `// TODO: Implement {service} integration after service registration`

    For methods that do exist but are misnamed, update the calls to match actual method names.
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | grep -c "analytics.ts.*does not exist" || echo "0"</automated>
  </verify>
  <done>Analytics routes compile without missing service method errors</done>
</task>

<task type="auto">
  <name>Fix reports route service method calls</name>
  <files>src/api/routes/reports.ts</files>
  <action>
    Fix the missing service method calls in reports routes to match actual ServiceFactory interface.

    Current errors (lines 105, 107, 108):
    - `serviceFactory.getWorkItemRepository()` - method doesn't exist
    - `serviceFactory.getSprintService()` - method doesn't exist
    - `serviceFactory.getGroupingService()` - method doesn't exist

    Apply same fix strategy as analytics routes:
    1. Check ServiceFactory.ts for actual available methods
    2. Replace missing method calls with:
       - TODO comments for future implementation
       - Placeholder responses that maintain route structure
       - Alternative service access if available

    Focus on compilation success rather than full functionality - reports are not critical for export engine.
    Maintain route structure but disable problematic service integrations temporarily.
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | grep -c "reports.ts.*does not exist" || echo "0"</automated>
  </verify>
  <done>Reports routes compile without missing service method errors</done>
</task>

<task type="auto">
  <name>Fix service layer method signature mismatches</name>
  <files>src/services/AnalyticsService.ts, src/services/ReportingService.ts, src/services/ReadinessService.ts</files>
  <action>
    Fix method signature and type compatibility issues in service layer.

    Key issues to address:
    1. **AnalyticsService.ts**: Method name mismatches (getSprintById vs getSprints, getAllSprints vs getSprints)
    2. **ReportingService.ts**: Missing properties on SprintBlocker type, wrong return types
    3. **ReadinessService.ts**: WorkItem type compatibility, missing audit event types

    For each service:

    **AnalyticsService.ts fixes:**
    - Replace `getSprintById` calls with available method names
    - Replace `getAllSprints` with actual available method
    - Fix WorkItem.specification property access (add optional chaining or alternative)
    - Fix return type mismatches (WorkItem[] vs paginated results)

    **ReportingService.ts fixes:**
    - Replace missing SprintBlocker properties with available ones
    - Fix method name mismatches similar to AnalyticsService
    - Handle WorkItem.specification access properly

    **ReadinessService.ts fixes:**
    - Fix WorkItem constructor calls to match actual interface
    - Add missing audit event types to AuditTrailService events
    - Fix type compatibility for readiness operations

    Focus on compilation - functionality can be adjusted later if needed.
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | grep -c -E "(AnalyticsService|ReportingService|ReadinessService)\.ts.*error" || echo "0"</automated>
  </verify>
  <done>Service layer compiles without method signature or type compatibility errors</done>
</task>

</tasks>

<verification>
Run `npm run typecheck` to verify clean compilation across all services
Confirm server can import all routes including exports without compilation errors
Validate ServiceFactory method calls match actual implementations
Test server startup to ensure no runtime compilation failures
</verification>

<success_criteria>
All service layer files compile without TypeScript errors
Analytics and reports routes use available ServiceFactory methods
Server compilation includes export routes successfully
Export engine unblocked from service layer compilation issues
</success_criteria>

<output>
After completion, create `.planning/phases/06-export-engine/06-05-SUMMARY.md`
</output>