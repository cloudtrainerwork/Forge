---
phase: 06-export-engine
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified: [
  "src/services/ExportService.ts",
  "src/adapters/IExportService.ts",
  "src/factories/ServiceFactory.ts"
]
autonomous: true
requirements: [EXPORT-01, EXPORT-02, EXPORT-05]
user_setup: []

must_haves:
  truths:
    - "ExportService can export work item specifications to GSD XML format"
    - "Export operations complete within 5 seconds per requirement"
    - "Service integrates with existing IoC container and audit trail patterns"
  artifacts:
    - path: "src/adapters/IExportService.ts"
      provides: "Clean interface for export operations"
      exports: ["IExportService"]
    - path: "src/services/ExportService.ts"
      provides: "Business service for GSD XML export"
      exports: ["ExportService"]
    - path: "src/factories/ServiceFactory.ts"
      provides: "IoC container registration for ExportService"
      contains: "IExportService binding"
  key_links:
    - from: "src/services/ExportService.ts"
      to: "src/services/GSDXmlGenerator.ts"
      via: "dependency injection for XML generation"
      pattern: "@inject.*GSDXmlGenerator"
    - from: "src/services/ExportService.ts"
      to: "ISpecificationService"
      via: "specification retrieval for export"
      pattern: "ISpecificationService.*getSpecification"
    - from: "src/factories/ServiceFactory.ts"
      to: "src/services/ExportService.ts"
      via: "IoC container binding"
      pattern: "bind.*IExportService.*ExportService"
---

<objective>
Create ExportService business layer with performance guarantees and audit trail integration

Purpose: Provide clean service interface for GSD XML export operations with 5-second performance guarantee and full audit logging
Output: Complete export service integrated with IoC container and ready for API consumption
</objective>

<execution_context>
@/Users/briannielsen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/briannielsen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-export-engine/06-RESEARCH.md

# Plan Dependencies
@.planning/phases/06-export-engine/06-01-SUMMARY.md

# Existing Service Patterns
@src/services/SpecificationService.ts
@src/factories/ServiceFactory.ts
@src/services/AuditTrailService.ts

# Interfaces Required
@src/domain/entities/GSDPlan.ts
@src/services/GSDXmlGenerator.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create IExportService interface</name>
  <files>src/adapters/IExportService.ts</files>
  <action>
    Create clean interface for export operations following established adapter patterns:
    - exportWorkItemToGSD(workItemId: string): Promise<ExportResult> method
    - ExportResult interface with filename, buffer, contentType, metadata properties
    - Include JSDoc documentation for all methods and interfaces
    - Follow naming conventions established in ISpecificationService
    - Include proper TypeScript return types for file download operations
    - Add error handling method signatures for failed exports
    - Include metadata about export operations (timestamp, file size, export type)

    Match existing interface patterns from ISpecificationService and IWorkItemRepository.
  </action>
  <verify>
    <automated>npm run type-check src/adapters/IExportService.ts</automated>
  </verify>
  <done>IExportService interface exists with proper method signatures and TypeScript types</done>
</task>

<task type="auto">
  <name>Task 2: Create ExportService implementation</name>
  <files>src/services/ExportService.ts</files>
  <action>
    Create ExportService following established service patterns:
    - Use @injectable decorator and constructor dependency injection
    - Inject ISpecificationService, GSDXmlGenerator, AuditTrailService dependencies
    - Implement exportWorkItemToGSD method with 5-second performance requirement
    - Include error handling for missing specifications and XML generation failures
    - Generate filename with pattern: "forge-workitem-{workItemId}-{timestamp}.xml"
    - Return Buffer.from() for XML content with proper content-type headers
    - Add audit trail logging for all export operations with metadata
    - Include validation: reject export for empty specifications
    - Add performance monitoring and timeout handling
    - Follow error message patterns from SpecificationService

    Use existing patterns from SpecificationService for validation, audit logging, and error handling.
  </action>
  <verify>
    <automated>npm test src/services/ExportService.test.ts -t "exports GSD XML"</automated>
  </verify>
  <done>ExportService exists with dependency injection and 5-second performance guarantee</done>
</task>

<task type="auto">
  <name>Task 3: Register ExportService in IoC container</name>
  <files>src/factories/ServiceFactory.ts</files>
  <action>
    Register ExportService in IoC container following established patterns:
    - Add IExportService interface import
    - Add ExportService class import
    - Bind IExportService to ExportService with singleton scope in configure() method
    - Add getExportService(): IExportService convenience method
    - Include ExportService in health check monitoring
    - Maintain alphabetical ordering of service registrations
    - Follow exact binding pattern used for ISpecificationService
    - Update type imports and maintain clean import organization

    Match the exact pattern used for SpecificationService registration in ServiceFactory.
  </action>
  <verify>
    <automated>npm test src/factories/ServiceFactory.test.ts -t "can resolve ExportService"</automated>
  </verify>
  <done>ExportService registered in IoC container with singleton lifecycle and health monitoring</done>
</task>

</tasks>

<verification>
- IExportService interface defines clean contract for export operations
- ExportService implements interface with proper dependency injection
- Service integrates with existing SpecificationService and audit patterns
- IoC container properly resolves ExportService dependencies
- Performance requirements met with 5-second export guarantee
</verification>

<success_criteria>
- IExportService interface exists with proper method signatures and return types
- ExportService implements business logic with dependency injection and audit logging
- ServiceFactory resolves ExportService through IoC container with singleton scope
- Export operations complete within 5 seconds with proper error handling
- All exports logged to audit trail with metadata for tracking
</success_criteria>

<output>
After completion, create `.planning/phases/06-export-engine/06-02-SUMMARY.md`
</output>