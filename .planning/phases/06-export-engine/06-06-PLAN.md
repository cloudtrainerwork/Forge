---
phase: 06-export-engine
plan: 06
type: execute
wave: 2
depends_on: [06-05]
files_modified: [src/services/AnalyticsService.ts, src/services/ReportingService.ts, src/services/ReadinessService.ts]
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "Service layer compiles without method signature or type compatibility errors"
    - "Server can start successfully with all routes registered including exports"
    - "Export functionality becomes accessible once server can start successfully"
  artifacts:
    - path: "src/services/AnalyticsService.ts"
      provides: "Fixed method signatures and type compatibility"
      min_lines: 500
    - path: "src/services/ReportingService.ts"
      provides: "Corrected service method calls and type definitions"
      min_lines: 400
    - path: "src/services/ReadinessService.ts"
      provides: "Fixed WorkItem compatibility and audit event types"
      min_lines: 300
  key_links:
    - from: "src/api/routes/analytics.ts"
      to: "src/services/AnalyticsService.ts"
      via: "Service dependency injection"
      pattern: "AnalyticsService"
    - from: "src/api/routes/reports.ts"
      to: "src/services/ReportingService.ts"
      via: "Service dependency injection"
      pattern: "ReportingService"
---

<objective>
Fix service layer method signature mismatches and type compatibility issues

Purpose: Resolve compilation issues in service implementations that block server startup
Output: Clean service layer compilation enabling complete server startup
</objective>

<execution_context>
@/Users/briannielsen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/briannielsen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-export-engine/06-VERIFICATION.md
@.planning/phases/06-export-engine/06-05-PLAN.md

# Service layer compilation errors that block server startup
# - AnalyticsService: Method name mismatches and missing properties
# - ReportingService: Type compatibility and method signature issues
# - ReadinessService: WorkItem interface compatibility and audit event types

<interfaces>
<!-- Service patterns that routes depend on -->

Expected service interface patterns:
```typescript
// Analytics services typically have:
getAnalytics(): Promise<Analytics[]>
// Not: getSprintById, getAllSprints

// Reporting services typically have:
getReports(): Promise<Report[]>
// Not: getGroupById, getAllGroups, getGroupForWorkItem

// WorkItem interface compatibility:
export class WorkItem {
  specification?: SpecificationTemplate
  // Optional property to handle missing specifications
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix AnalyticsService method signature mismatches</name>
  <files>src/services/AnalyticsService.ts</files>
  <action>
    Fix method signature and type compatibility issues in AnalyticsService.

    Key issues to address:
    1. **Method name mismatches**: Replace `getSprintById` calls with available method names
    2. **Missing methods**: Replace `getAllSprints` with actual available method
    3. **Property access**: Fix WorkItem.specification property access (add optional chaining)
    4. **Return types**: Fix return type mismatches (WorkItem[] vs paginated results)

    Specific fixes:
    - Replace method calls that don't exist on sprint/grouping services
    - Add optional chaining for WorkItem.specification property access
    - Ensure return types match interface contracts
    - Use placeholder implementations for missing functionality with TODO comments

    Focus on compilation success - functionality can be refined later if needed.
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | grep -c "AnalyticsService.ts.*error" || echo "0"</automated>
  </verify>
  <done>AnalyticsService compiles without method signature or property access errors</done>
</task>

<task type="auto">
  <name>Task 2: Fix ReportingService and ReadinessService compatibility issues</name>
  <files>src/services/ReportingService.ts, src/services/ReadinessService.ts</files>
  <action>
    Fix remaining service layer compilation issues.

    **ReportingService.ts fixes:**
    - Replace missing SprintBlocker properties with available ones
    - Fix method name mismatches similar to AnalyticsService
    - Handle WorkItem.specification access properly with optional chaining
    - Ensure proper return types for report generation

    **ReadinessService.ts fixes:**
    - Fix WorkItem constructor calls to match actual interface
    - Add missing audit event types to AuditTrailService events
    - Fix type compatibility for readiness operations
    - Handle specification property access consistently

    Apply same strategy as AnalyticsService: focus on compilation, use placeholder implementations with TODOs for missing functionality.
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | grep -c -E "(ReportingService|ReadinessService)\.ts.*error" || echo "0"</automated>
  </verify>
  <done>ReportingService and ReadinessService compile without type compatibility errors</done>
</task>

</tasks>

<verification>
- Run `npm run typecheck` to verify clean compilation across all services
- Confirm server can import all services without compilation errors
- Validate service method calls match actual implementations
- Test server startup to ensure no runtime compilation failures
</verification>

<success_criteria>
- All service layer files compile without TypeScript errors
- Service method signatures match available interface implementations
- Server compilation includes all services successfully
- Export engine unblocked from service layer compilation issues
</success_criteria>

<output>
After completion, create `.planning/phases/06-export-engine/06-06-SUMMARY.md`
</output>