---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  docker-compose.yml,
  package.json,
  .env.example,
  .env,
  .gitignore,
  src/config/database.ts,
  src/config/environment.ts
]
autonomous: true

must_haves:
  truths:
    - "Neo4j database is accessible on ports 7474 and 7687"
    - "PostgreSQL database is accessible and accepts connections"
    - "Environment variables are properly configured and validated"
    - "Project structure supports TypeScript and enterprise patterns"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Database containers with production-grade configuration"
      contains: "neo4j and postgres services"
    - path: "package.json"
      provides: "Backend dependencies with TypeScript support"
      exports: ["scripts"]
    - path: "src/config/database.ts"
      provides: "Database connection factory"
      exports: ["createNeo4jDriver", "createPrismaClient"]
    - path: "src/config/environment.ts"
      provides: "Environment configuration validation"
      exports: ["config"]
  key_links:
    - from: "docker-compose.yml"
      to: "environment variables"
      via: "NEO4J_AUTH and POSTGRES_* vars"
      pattern: "NEO4J_AUTH.*neo4j.*password"
    - from: "src/config/database.ts"
      to: "process.env"
      via: "environment variable access"
      pattern: "process\\.env\\.(NEO4J|DATABASE)_"
---

<objective>
Establish the foundational infrastructure for FORGE, creating Docker-based development environment with Neo4j and PostgreSQL databases, TypeScript backend project structure, and enterprise-grade configuration management.

Purpose: Prevents critical infrastructure pitfalls early by establishing production-grade database setup from the start
Output: Complete development environment with validated database connections and project scaffolding
</objective>

<execution_context>
@/Users/briannielsen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/briannielsen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Create Docker infrastructure with Neo4j and PostgreSQL</name>
  <files>docker-compose.yml, .env.example, .env</files>
  <action>
    Create docker-compose.yml with production-grade Neo4j and PostgreSQL services. Use Neo4j 5.x with authentication (neo4j/password), proper memory limits (512M), and persistent volumes. Configure PostgreSQL 15+ with connection pooling, environment-specific credentials (postgres/password/appdb per research), and proper initialization. Create .env.example and .env files with all required environment variables: NEO4J_URI, NEO4J_USER, NEO4J_PASSWORD, DATABASE_URL, POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB. Use research-recommended configuration to prevent production performance gaps.
  </action>
  <verify>docker-compose up -d && docker ps shows both services running healthy</verify>
  <done>Neo4j accessible at localhost:7474 and PostgreSQL accepting connections on port 5432</done>
</task>

<task type="auto">
  <name>Initialize TypeScript backend project structure</name>
  <files>package.json, .gitignore, src/config/, src/domain/, src/infrastructure/, src/adapters/, src/services/, src/factories/, src/observers/</files>
  <action>
    Create package.json for backend with neo4j-driver 5.x, @prisma/client, @prisma/adapter-pg, pg, inversify, class-transformer, class-validator, and dotenv per research standard stack. Add TypeScript dependencies (@types/node, @types/pg, prisma). Configure scripts for dev, build, and database operations. Create enterprise directory structure following research recommendations: src/domain (business entities), src/infrastructure (database connections), src/adapters (database interfaces), src/services (business logic), src/factories (IoC configuration), src/observers (event handling), src/config (environment setup). Create .gitignore excluding node_modules, .env, and build artifacts.
  </action>
  <verify>npm install completes without errors and directory structure matches enterprise patterns</verify>
  <done>Complete TypeScript project with research-recommended dependencies and enterprise directory structure</done>
</task>

<task type="auto">
  <name>Implement environment configuration and database connection factories</name>
  <files>src/config/environment.ts, src/config/database.ts</files>
  <action>
    Create src/config/environment.ts with schema validation for all required environment variables (NEO4J_URI, NEO4J_USER, NEO4J_PASSWORD, DATABASE_URL, POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB, NODE_ENV). Use dotenv with proper type checking and validation to prevent configuration errors. Create src/config/database.ts implementing Neo4jConnectionFactory and PrismaConnectionFactory per research code examples. Include connection verification, proper error handling, and singleton patterns. Implement graceful shutdown handlers for both database connections. Follow research guidance on connection pooling and production-grade configuration.
  </action>
  <verify>TypeScript compilation passes and connection factories can be imported without errors</verify>
  <done>Environment configuration validates all required variables and database factories ready for dependency injection</done>
</task>

</tasks>

<verification>
Run docker-compose up -d && npm install && npm run build to verify complete infrastructure setup
Neo4j dashboard accessible at http://localhost:7474 with neo4j/password credentials
PostgreSQL connection test succeeds with provided environment variables
TypeScript project compiles without errors and follows enterprise patterns
</verification>

<success_criteria>
Docker environment running with both databases accessible
Backend project structure supports enterprise patterns and future extensibility
All environment variables properly configured and validated
Database connection factories ready for dependency injection in subsequent plans
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>