---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified: [
  src/services/WorkItemService.ts,
  src/services/AuditTrailService.ts,
  src/factories/ServiceFactory.ts,
  src/observers/DataSynchronizationObserver.ts,
  src/main.ts,
  src/api/routes/workItems.ts,
  src/api/server.ts
]
autonomous: true

must_haves:
  truths:
    - "Work items can be created and stored in both Neo4j and PostgreSQL"
    - "Data synchronization maintains consistency between graph and relational stores"
    - "All user interactions are logged to audit trail"
    - "API endpoints accept work item operations and return proper responses"
  artifacts:
    - path: "src/services/WorkItemService.ts"
      provides: "Business logic for work item operations"
      exports: ["WorkItemService"]
    - path: "src/services/AuditTrailService.ts"
      provides: "Event handling and audit logging"
      exports: ["AuditTrailService"]
    - path: "src/factories/ServiceFactory.ts"
      provides: "IoC container configuration"
      exports: ["container", "createServiceFactory"]
    - path: "src/api/server.ts"
      provides: "Express server with work item endpoints"
      exports: ["app", "startServer"]
  key_links:
    - from: "src/services/WorkItemService.ts"
      to: "repositories"
      via: "dependency injection"
      pattern: "constructor.*Repository"
    - from: "src/observers/DataSynchronizationObserver.ts"
      to: "AuditTrailService"
      via: "event emission"
      pattern: "emit.*stateChange"
    - from: "src/api/routes/workItems.ts"
      to: "WorkItemService"
      via: "service method calls"
      pattern: "workItemService\\."
---

<objective>
Implement the service layer with business logic, event-driven data synchronization, IoC container configuration, and basic API endpoints, completing the backend foundation.

Purpose: Establishes data synchronization between Neo4j and PostgreSQL with enterprise patterns for maintainability
Output: Complete backend API with audit trail and synchronized dual-store operations
</objective>

<execution_context>
@/Users/briannielsen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/briannielsen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Implement business services with hybrid data operations</name>
  <files>src/services/WorkItemService.ts, src/services/AuditTrailService.ts</files>
  <action>
    Create WorkItemService following research HybridWorkItemService pattern with dependency injection for IWorkItemRepository and IGraphRepository. Implement createWorkItem (saves to PostgreSQL, syncs to Neo4j as node, publishes event), updateReadiness (updates PostgreSQL spec, publishes state change event), createDependency (creates Neo4j relationship, logs to audit trail). Create AuditTrailService extending EventEmitter per research pattern with event handlers for WORK_ITEM_CREATED, RELATIONSHIP_ADDED, READINESS_UPDATED. Handle event persistence to PostgreSQL audit log and proper error propagation. Use inversify decorators for dependency injection configuration.
  </action>
  <verify>Services compile and can be instantiated with proper dependency injection</verify>
  <done>Business services handle work item operations with event-driven dual-store synchronization</done>
</task>

<task type="auto">
  <name>Configure IoC container and data synchronization</name>
  <files>src/factories/ServiceFactory.ts, src/observers/DataSynchronizationObserver.ts</files>
  <action>
    Create ServiceFactory using inversify Container to configure dependency injection per research recommendations. Bind interfaces to implementations: IGraphRepository to GraphRepository, IWorkItemRepository to WorkItemRepository, WorkItemService, AuditTrailService. Configure singleton lifecycle for database connections and service instances. Create DataSynchronizationObserver implementing event handlers for PostgreSQLDataChanged and Neo4jRelationshipChanged per research DataSynchronizationService pattern. Handle sync failures with retry logic and compensation patterns to prevent dual-write consistency issues identified in research pitfalls.
  </action>
  <verify>IoC container resolves all dependencies and data sync observers handle events properly</verify>
  <done>Dependency injection configured and data synchronization prevents consistency issues between stores</done>
</task>

<task type="auto">
  <name>Create API endpoints and server setup</name>
  <files>src/api/server.ts, src/api/routes/workItems.ts, src/main.ts</files>
  <action>
    Create Express server in src/api/server.ts with middleware for JSON parsing, CORS, and error handling. Create work item routes in src/api/routes/workItems.ts with POST /work-items (creates work item with spec and readiness), GET /work-items/:id (retrieves with current state), PUT /work-items/:id/readiness (updates readiness dimensions), GET /work-items (lists with readiness filtering). Use dependency injection to access WorkItemService from routes. Implement proper error responses (400 for validation, 404 for not found, 500 for server errors). Create src/main.ts as entry point that initializes IoC container, starts database connections, and starts server. Include graceful shutdown handling for database connections.
  </action>
  <verify>Server starts successfully and API endpoints respond with proper status codes</verify>
  <done>Complete backend API with work item management and proper error handling</done>
</task>

</tasks>

<verification>
Run npm start to verify server starts and connects to both databases
POST to /work-items creates entries in both PostgreSQL and Neo4j
GET /work-items returns properly formatted work item data
Audit trail logs all state changes to PostgreSQL
Data synchronization maintains consistency between stores
</verification>

<success_criteria>
Backend API runs and accepts work item operations
Data synchronization works between Neo4j and PostgreSQL without consistency issues
All user interactions logged to audit trail for complete state change tracking
IoC container properly manages dependencies and enables easy testing
System handles errors gracefully and maintains data integrity
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>