---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [
  prisma/schema.prisma,
  src/domain/entities/WorkItem.ts,
  src/domain/entities/ReadinessState.ts,
  src/infrastructure/neo4j/GraphRepository.ts,
  src/infrastructure/postgresql/WorkItemRepository.ts,
  src/adapters/IGraphRepository.ts,
  src/adapters/IWorkItemRepository.ts
]
autonomous: true

must_haves:
  truths:
    - "Prisma schema defines WorkItem and AuditLog models with JSONB support"
    - "Neo4j repository can create nodes and relationships"
    - "PostgreSQL repository can store work item specifications with JSONB querying"
    - "Domain entities enforce 6-dimensional readiness structure"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Database schema for work items and audit trail"
      contains: "WorkItem and AuditLog models"
    - path: "src/domain/entities/WorkItem.ts"
      provides: "Core work item entity with readiness dimensions"
      exports: ["WorkItem", "ReadinessState"]
    - path: "src/infrastructure/neo4j/GraphRepository.ts"
      provides: "Neo4j operations for graph relationships"
      exports: ["GraphRepository"]
    - path: "src/infrastructure/postgresql/WorkItemRepository.ts"
      provides: "PostgreSQL operations for work item specs"
      exports: ["WorkItemRepository"]
  key_links:
    - from: "src/infrastructure/postgresql/WorkItemRepository.ts"
      to: "prisma client"
      via: "Prisma ORM calls"
      pattern: "prisma\\.(workItem|auditLog)\\."
    - from: "src/infrastructure/neo4j/GraphRepository.ts"
      to: "Neo4j session"
      via: "Cypher query execution"
      pattern: "session\\.run\\("
---

<objective>
Create the data layer foundation with Prisma schema, domain entities, and repository implementations for both Neo4j and PostgreSQL, establishing the hybrid architecture core.

Purpose: Implements the dual-store pattern essential for graph relationships and structured data persistence
Output: Complete data layer with repositories ready for service integration
</objective>

<execution_context>
@/Users/briannielsen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/briannielsen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Create Prisma schema for PostgreSQL data models</name>
  <files>prisma/schema.prisma</files>
  <action>
    Initialize Prisma with PostgreSQL provider and create schema.prisma. Define WorkItem model with id (cuid), spec (Json @db.JsonB for flexible work item specifications), readiness (Json @db.JsonB for 6-dimensional tracking), createdAt, updatedAt timestamps. Define AuditLog model with id, type (enum: WORK_ITEM_CREATED, RELATIONSHIP_ADDED, READINESS_UPDATED), entityId, changes (Json @db.JsonB), timestamp for complete audit trail per research requirements. Use JSONB for flexible schemas while maintaining structured core data. Configure PostgreSQL adapter as recommended in research.
  </action>
  <verify>prisma generate completes and creates TypeScript client types</verify>
  <done>Prisma schema supports work item specs, readiness tracking, and audit trail with JSONB flexibility</done>
</task>

<task type="auto">
  <name>Implement domain entities with readiness dimensions</name>
  <files>src/domain/entities/WorkItem.ts, src/domain/entities/ReadinessState.ts</files>
  <action>
    Create WorkItem domain entity with id, title, description, spec (flexible object), readiness (ReadinessState), timestamps. Create ReadinessState entity enforcing 6-dimensional structure: Requirements (enum: NOT_STARTED, IN_PROGRESS, COMPLETE), Design (enum), Frontend (enum), Backend (enum), Integration (enum), Test (enum). Each dimension tracks progress and completion state. Include validation methods to prevent incomplete nodes from entering ready state per phase requirements. Use class-transformer decorators for serialization and class-validator for input validation as recommended in research.
  </action>
  <verify>TypeScript compilation passes and entities can be instantiated with proper validation</verify>
  <done>Domain entities enforce 6-dimensional readiness structure with validation rules</done>
</task>

<task type="auto">
  <name>Create repository implementations for hybrid data access</name>
  <files>src/adapters/IGraphRepository.ts, src/adapters/IWorkItemRepository.ts, src/infrastructure/neo4j/GraphRepository.ts, src/infrastructure/postgresql/WorkItemRepository.ts</files>
  <action>
    Create IGraphRepository interface defining createNode, createRelationship, traverseDependencies, findNodesByType methods. Create IWorkItemRepository interface defining save, findById, findByReadiness, logStateChange methods. Implement GraphRepository using Neo4j driver with proper session management, Cypher query execution for node/relationship CRUD, and error handling per research examples. Implement WorkItemRepository using Prisma client with JSONB operations, spec querying, readiness filtering, and audit logging. Follow research pattern for hybrid data repository with separate concerns for graph vs document storage.
  </action>
  <verify>Repository classes compile and can be instantiated with database connections</verify>
  <done>Repository implementations ready for dependency injection with full CRUD operations for both data stores</done>
</task>

</tasks>

<verification>
Run prisma generate && prisma db push to create database tables
TypeScript compilation completes without errors for all domain entities and repositories
Repository interfaces properly abstract database-specific implementations
Database schema supports JSONB querying and 6-dimensional readiness structure
</verification>

<success_criteria>
Prisma schema created and database tables exist in PostgreSQL
Domain entities enforce business rules for 6-dimensional readiness tracking
Repository pattern implemented for both Neo4j and PostgreSQL with proper abstractions
Data layer ready for service integration without exposing database specifics
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>