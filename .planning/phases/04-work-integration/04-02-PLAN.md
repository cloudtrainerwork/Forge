---
phase: 04-work-integration
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: [
  frontend/src/utils/api.ts,
  frontend/src/components/ForgeDetailViewEnhanced.tsx,
  frontend/src/hooks/useOfflineQueue.ts
]
autonomous: false

must_haves:
  truths:
    - "System handles network failures gracefully without losing user work"
    - "User receives clear feedback when operations fail and can retry"
    - "Graph canvas performance remains smooth with 500+ nodes as specified"
    - "Changes are queued offline and sync automatically when connection restored"
    - "All integration points work correctly under real-world conditions"
  artifacts:
    - path: "frontend/src/utils/api.ts"
      provides: "Robust API client with error handling and retry logic"
      contains: "try.*catch.*error"
    - path: "frontend/src/hooks/useOfflineQueue.ts"
      provides: "Offline capability for work item operations"
      exports: ["useOfflineQueue"]
    - path: "frontend/src/components/ForgeDetailViewEnhanced.tsx"
      provides: "Error-resilient ReactFlow component with user feedback"
      contains: "error.*state"
  key_links:
    - from: "frontend/src/components/ForgeDetailViewEnhanced.tsx"
      to: "frontend/src/hooks/useOfflineQueue.ts"
      via: "offline queue hook usage"
      pattern: "useOfflineQueue"
    - from: "frontend/src/hooks/useOfflineQueue.ts"
      to: "localStorage"
      via: "queue persistence"
      pattern: "localStorage\\.(get|set)Item"
---

<objective>
Add robustness and performance validation to the ReactFlow-backend integration, ensuring production-ready reliability and meeting 500+ node performance requirements.

Purpose: Complete the integration work by adding error handling, offline capability, and performance validation per user's focus on backend integration
Output: Production-ready work integration system with robust error handling and validated performance
</objective>

<execution_context>
@/Users/briannielsen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/briannielsen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-work-integration/04-CONTEXT.md
@.planning/phases/04-work-integration/04-01-SUMMARY.md
@frontend/src/utils/api.ts
@frontend/src/components/ForgeDetailViewEnhanced.tsx
</context>

<tasks>

<task type="auto">
  <name>Add comprehensive error handling and offline capability</name>
  <files>
    frontend/src/utils/api.ts
    frontend/src/hooks/useOfflineQueue.ts
    frontend/src/components/ForgeDetailViewEnhanced.tsx
  </files>
  <action>
    Enhance the integration with production-ready error handling and offline support:

    1. Create useOfflineQueue.ts hook for offline capability:
       - Queue failed operations in localStorage with retry metadata
       - Automatically retry when network connection restored
       - Provide status indicators (online, offline, syncing, errors)
       - Handle queue persistence across browser sessions

    2. Update api.ts with comprehensive error handling:
       - Wrap all fetch calls in try/catch with proper error categorization
       - Distinguish between network errors (queue for retry) and validation errors (show immediately)
       - Add exponential backoff for retry logic
       - Include proper HTTP status handling (400, 401, 403, 404, 500, etc.)
       - Add request timeouts to prevent hanging operations

    3. Update ForgeDetailViewEnhanced.tsx for error resilience:
       - Integrate useOfflineQueue hook for all data operations
       - Add error state management and user feedback UI
       - Show loading states for async operations
       - Display retry buttons for failed operations
       - Add success/error toast notifications or similar feedback
       - Implement optimistic updates with rollback on error

    4. Add network status detection:
       - Monitor online/offline status
       - Pause queue operations when offline
       - Resume and sync when connection restored
       - Show appropriate UI indicators for connection status

    Follow established patterns from Phase 3 readiness system for error handling consistency.
  </action>
  <verify>Network disconnection test shows queued operations, reconnection triggers sync, error states display properly</verify>
  <done>Production-ready error handling implemented, offline queue functional, user feedback clear for all states</done>
</task>

<task type="auto">
  <name>Performance testing and large graph optimization</name>
  <files>frontend/src/components/ForgeDetailViewEnhanced.tsx</files>
  <action>
    Implement and test performance optimizations for 500+ node requirement:

    1. Add performance monitoring:
       - Measure ReactFlow render times for large node sets
       - Track memory usage during graph operations
       - Log performance metrics to console in development mode
       - Identify bottlenecks in data loading and rendering

    2. Implement progressive loading patterns:
       - Load nodes in batches rather than all at once for large screens
       - Add virtualization for node rendering if needed
       - Implement level-of-detail rendering for distant nodes
       - Cache frequently accessed data transformations

    3. Optimize ReactFlow configuration:
       - Configure appropriate node and edge defaults for performance
       - Use React.memo for custom node components if not already done
       - Minimize re-renders through proper dependency arrays
       - Optimize edge pathfinding for large graphs

    4. Add memory management:
       - Clean up event listeners and subscriptions on unmount
       - Implement proper garbage collection for removed nodes
       - Monitor for memory leaks in large graph scenarios

    5. Test with realistic data volume:
       - Create test scenarios with 500+ nodes and complex dependencies
       - Verify smooth pan/zoom operations at scale
       - Ensure acceptable load times for large screens

    Reference Phase 2 performance patterns (WebGL rendering thresholds, progressive loading, memory monitoring) for consistency.
  </action>
  <verify>Load test with 500+ nodes completes successfully, pan/zoom remains smooth, memory usage stays within acceptable limits</verify>
  <done>Performance validated for 500+ nodes, optimizations implemented, no memory leaks or lag detected</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete ReactFlow-backend integration with error handling and performance optimization for Phase 4 work integration</what-built>
  <how-to-verify>
    1. Start the application: `npm run dev` from frontend directory
    2. Open http://localhost:3000 in browser
    3. Create a new work item by dragging from palette - verify it appears in graph and persists
    4. Edit work item properties - verify changes save to backend immediately
    5. Create dependencies between nodes - verify edges persist and reload correctly
    6. Test offline functionality:
       - Disconnect network (turn off wifi or use dev tools)
       - Make changes to nodes - verify they queue for sync
       - Reconnect network - verify queued changes sync automatically
    7. Test error scenarios:
       - Stop backend server (Ctrl+C in backend terminal)
       - Try to create node - verify error feedback and retry options
       - Restart backend - verify automatic reconnection
    8. Test performance with large graph:
       - Create or load screen with 100+ nodes if possible
       - Verify smooth pan/zoom operations
       - Check browser dev tools for memory leaks or performance warnings
  </how-to-verify>
  <resume-signal>Type "approved" if integration works correctly, or describe any issues for fixes</resume-signal>
</task>

</tasks>

<verification>
- ReactFlow frontend fully integrated with backend APIs
- Work items and dependencies persist correctly to databases
- Error handling and offline capability functional
- Performance acceptable with large node counts
- User feedback clear for all operation states
- No data loss during network interruptions
</verification>

<success_criteria>
Complete Phase 4 success criteria achieved: Users can create and edit work items as fully functional graph nodes with real-time backend synchronization, robust error handling, and validated performance at scale.
</success_criteria>

<output>
After completion, create `.planning/phases/04-work-integration/04-02-SUMMARY.md`
</output>