---
phase: 04-work-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  frontend/src/utils/api.ts,
  frontend/src/components/ForgeDetailViewEnhanced.tsx
]
autonomous: true

must_haves:
  truths:
    - "User can create work items in ReactFlow and they persist to backend database"
    - "User can edit work item properties and changes sync to backend immediately"
    - "User can create dependency relationships that save to Neo4j graph database"
    - "ReactFlow nodes display real readiness data from backend services"
    - "Graph canvas loads existing work items from backend on screen load"
  artifacts:
    - path: "frontend/src/utils/api.ts"
      provides: "ReactFlow-compatible API client with data transformations"
      exports: ["saveWorkItems", "loadWorkItems", "saveDependencies", "loadDependencies"]
    - path: "frontend/src/components/ForgeDetailViewEnhanced.tsx"
      provides: "Production ReactFlow component with backend integration"
      contains: "useEffect.*loadScreenData"
  key_links:
    - from: "frontend/src/components/ForgeDetailViewEnhanced.tsx"
      to: "frontend/src/utils/api.ts"
      via: "saveScreenData calls"
      pattern: "saveScreenData.*nodes.*edges"
    - from: "frontend/src/utils/api.ts"
      to: "/api/v1/work-items"
      via: "fetch calls"
      pattern: "fetch.*v1/work-items"
    - from: "src/api/routes/workItems.ts"
      to: "Neo4j database"
      via: "WorkItemService"
      pattern: "workItemService\\.(create|update)"
---

<objective>
Connect ReactFlow frontend to backend services, enabling real data persistence and synchronization for work items and dependencies.

Purpose: Honor user decision to leverage ForgeDetailViewEnhanced.tsx by connecting it to backend APIs instead of reverting to Cytoscape.js
Output: Production-ready ReactFlow graph canvas with full backend integration
</objective>

<execution_context>
@/Users/briannielsen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/briannielsen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-work-integration/04-CONTEXT.md
@.planning/phases/03-readiness-system/03-04-SUMMARY.md
@frontend/src/components/ForgeDetailViewEnhanced.tsx
@frontend/src/utils/api.ts
@src/api/routes/workItems.ts
@src/services/WorkItemService.ts
</context>

<tasks>

<task type="auto">
  <name>Update API client for ReactFlow data transformations</name>
  <files>frontend/src/utils/api.ts</files>
  <action>
    Update the API client to handle ReactFlow Node and Edge data structures properly:

    1. **Remove Math.random() mock readiness data (lines 134-139)**:
       - Replace Math.random() * 100 for each readiness dimension with actual backend data
       - Use node.data.readiness from ReactFlow node if available, otherwise use 0 as default
       - Preserve readiness structure: {requirements, design, frontend, backend, integration, test}

    2. **Add specific ReactFlow Node transformation functions**:
       - `transformReactFlowToWorkItem(node: Node)`: Convert ReactFlow Node {id, type, position: {x, y}, data: {label, readiness, currentState, notes, templateKey}} to backend WorkItem format
       - `transformWorkItemToReactFlow(workItem: WorkItem)`: Convert backend WorkItem to ReactFlow Node format with proper position and data structure
       - Handle all ReactFlow-specific properties: position.x/y, data object structure, type field

    3. **Add ReactFlow Edge transformation functions**:
       - `transformReactFlowToeDependency(edge: Edge)`: Convert ReactFlow Edge {id, source, target, type, markerEnd} to backend Dependency format
       - `transformDependencyToReactFlowEdge(dependency: Dependency)`: Convert backend Dependency to ReactFlow Edge with proper markerEnd and styling

    4. Update saveWorkItems to accept ReactFlow Node[] and transform each node before sending to backend

    5. Update loadWorkItems to transform backend WorkItem[] to ReactFlow Node[] format with proper position and data structure

    6. Add saveDependencies and loadDependencies functions for ReactFlow Edge handling

    7. Replace any remaining mock data with real API calls per user decision to focus on backend integration

    8. Add proper error handling for network failures but keep it simple (will enhance in next plan)

    Use existing API_BASE_URL and endpoints - do not change the backend API structure.
  </action>
  <verify>npm test passes, API functions transform data correctly between ReactFlow and backend formats, no Math.random() calls remain</verify>
  <done>API client converts between ReactFlow and backend data structures, Math.random() mock data removed, real backend calls implemented</done>
</task>

<task type="auto">
  <name>Connect ForgeDetailViewEnhanced to real backend APIs</name>
  <files>frontend/src/components/ForgeDetailViewEnhanced.tsx</files>
  <action>
    Update ForgeDetailViewEnhanced component to use real backend APIs per user decision:

    1. Replace any sample/mock data usage with real API calls via updated api.ts functions

    2. Add useEffect hook to load existing screen data on component mount:
       - Call loadScreenData(screenId) to get work items and dependencies
       - Transform to ReactFlow format and set as initial nodes/edges
       - Handle empty state gracefully (new screen with no data)

    3. Update onSave callback to persist changes immediately:
       - Convert ReactFlow nodes to WorkItem format using transformReactFlowToWorkItem
       - Convert ReactFlow edges to Dependency format using transformReactFlowToDependency
       - Call saveScreenData with transformed data
       - Use optimistic updates (update UI first, handle errors if they occur)

    4. Ensure readiness tracking displays real backend data:
       - Readiness percentages from backend WorkItem.readiness object (not Math.random())
       - Visual indicators reflect actual stored values, not hardcoded
       - Update readiness display to use actual data from node.data.readiness

    5. Update node creation to generate proper IDs and include screenId in data

    6. Maintain existing ReactFlow functionality (drag, edit, grouping) per user decision to accept ReactFlow implementation

    Do not change ReactFlow structure or revert to Cytoscape - honor user's locked decision.
  </action>
  <verify>Component loads real data on mount, saves changes to backend, readiness indicators show backend data instead of random values</verify>
  <done>ForgeDetailViewEnhanced connected to backend, real data persistence working, no mock data remaining, readiness shows actual backend values</done>
</task>

</tasks>

<verification>
- React app starts without errors
- Graph canvas loads existing work items from backend
- Creating new nodes saves to backend database
- Editing node properties persists changes
- Creating dependency edges saves to Neo4j
- Readiness indicators display real backend data (no random values)
- No console errors related to data format mismatches
</verification>

<success_criteria>
ForgeDetailViewEnhanced component fully integrated with backend services, enabling real-time work item management with ReactFlow interface as requested by user decisions.
</success_criteria>

<output>
After completion, create `.planning/phases/04-work-integration/04-01-SUMMARY.md`
</output>