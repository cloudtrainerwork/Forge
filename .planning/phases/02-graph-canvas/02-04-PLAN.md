---
phase: 02-graph-canvas
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [frontend/src/lib/graphApi.ts]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Canvas connects to backend API for data fetching"
    - "Frontend can load work items from backend successfully"
    - "API integration works with correct endpoint configuration"
  artifacts:
    - path: "frontend/src/lib/graphApi.ts"
      provides: "API client with correct endpoint configuration"
      exports: ["fetchWorkItems", "createWorkItem", "getWorkItem"]
  key_links:
    - from: "frontend/src/lib/graphApi.ts"
      to: "backend /api/v1/work-items"
      via: "fetch calls to correct endpoint"
      pattern: "\/api\/v1\/work-items"
---

<objective>
Fix API endpoint mismatch to restore backend connectivity for the graph canvas.

Purpose: Resolve the configuration gap preventing frontend from connecting to backend APIs, enabling real data loading instead of sample data fallback.
Output: Working API integration allowing graph canvas to load and display actual work items from the backend.
</objective>

<execution_context>
@/Users/briannielsen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/briannielsen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 2 context - API integration already built, just needs endpoint fix
@.planning/phases/02-graph-canvas/02-01-SUMMARY.md
@frontend/src/lib/graphApi.ts
</context>

<tasks>

<task type="auto">
  <name>Fix API endpoint configuration in graphApi.ts</name>
  <files>frontend/src/lib/graphApi.ts</files>
  <action>
    Update the API_ENDPOINTS configuration to use correct backend endpoint paths:

    - Change '/api/work-items' to '/api/v1/work-items' in workItems endpoint
    - Change '/api/work-items/${id}' to '/api/v1/work-items/${id}' in workItem endpoint
    - Change '/api/work-items/${id}/readiness' to '/api/v1/work-items/${id}/readiness' in readiness endpoint
    - Change '/api/work-items/${id}/dependencies' to '/api/v1/work-items/${id}/dependencies' in dependencies endpoint
    - Update checkApiHealth function to use '/api/v1/work-items?limit=1' for health check

    Gap reason: Frontend configured for '/api/work-items' but backend actually serves on '/api/v1/work-items' (verified in src/api/server.ts line 70)

    Reference existing code: API client with graceful fallback and error handling already implemented in Plan 02-01, just need endpoint path correction
  </action>
  <verify>
    Test API integration:
    1. Start backend server: cd /Users/briannielsen/forge && npm run backend:dev
    2. Start frontend: cd /Users/briannielsen/forge/frontend && npm run dev
    3. Visit http://localhost:3000 and check browser network tab for successful requests to /api/v1/work-items
    4. Verify no 404 errors for work items API calls
  </verify>
  <done>
    API_ENDPOINTS object uses '/api/v1/work-items' pattern consistently, frontend successfully connects to backend API, graph canvas loads real work items instead of sample data
  </done>
</task>

<task type="auto">
  <name>Test backend API integration</name>
  <files>frontend/src/lib/graphApi.ts</files>
  <action>
    Run integration test to ensure API connectivity works end-to-end:

    - Start backend service if not running
    - Execute test API calls using updated endpoints
    - Verify successful responses from all API endpoints (work items, readiness, dependencies)
    - Check that checkApiHealth() returns true when backend is running
    - Test error handling still works correctly with new endpoints

    This ensures the endpoint fix resolves the verification gap completely.
  </action>
  <verify>
    Run: cd /Users/briannielsen/forge/frontend && npm test -- graphApi
    All API integration tests pass, health check succeeds, no endpoint-related errors
  </verify>
  <done>
    Backend API integration test passes, all endpoints respond correctly, health check returns true, frontend can successfully load work items from backend
  </done>
</task>

</tasks>

<verification>
Canvas connects to backend API for data fetching (Truth #2 from verification gaps)
- Frontend loads work items from actual backend instead of falling back to sample data
- Network requests in browser devtools show successful 200 responses to /api/v1/work-items
- Graph canvas displays real work item data, not hardcoded sample data
</verification>

<success_criteria>
API endpoint mismatch resolved and backend connectivity restored:
1. graphApi.ts uses '/api/v1/work-items' endpoints matching backend configuration
2. Frontend successfully fetches work items from backend when both services running
3. No more 404 errors in browser network tab for work items API calls
4. Graph canvas loads and displays actual backend data instead of sample data fallback
5. API health check passes when backend is available
</success_criteria>

<output>
After completion, create `.planning/phases/02-graph-canvas/02-04-SUMMARY.md`
</output>