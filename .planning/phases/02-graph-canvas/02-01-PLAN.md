---
phase: 02-graph-canvas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "frontend/package.json",
  "frontend/next.config.js",
  "frontend/tsconfig.json",
  "frontend/src/app/layout.tsx",
  "frontend/src/app/page.tsx",
  "frontend/src/components/GraphCanvas.tsx",
  "frontend/src/lib/graphApi.ts",
  "frontend/tailwind.config.js"
]
autonomous: true

must_haves:
  truths:
    - "Frontend application loads with basic graph canvas visible"
    - "Canvas connects to backend API for data fetching"
    - "Cytoscape.js renders initial graph elements"
  artifacts:
    - path: "frontend/src/components/GraphCanvas.tsx"
      provides: "Cytoscape.js canvas component with WebGL rendering"
      min_lines: 80
    - path: "frontend/src/lib/graphApi.ts"
      provides: "API client for backend integration"
      exports: ["fetchWorkItems", "createWorkItem", "updateWorkItem"]
    - path: "frontend/package.json"
      provides: "Frontend dependencies including Cytoscape.js"
      contains: "cytoscape"
  key_links:
    - from: "frontend/src/components/GraphCanvas.tsx"
      to: "cytoscape library"
      via: "import and initialization"
      pattern: "import.*cytoscape"
    - from: "frontend/src/lib/graphApi.ts"
      to: "backend API"
      via: "fetch to work items endpoints"
      pattern: "fetch.*api/work-items"
---

<objective>
Establish Next.js frontend application with Cytoscape.js graph canvas foundation, integrating with Phase 1 backend APIs.

Purpose: Create the frontend platform for interactive graph visualization with production-grade performance optimization and memory-safe rendering capabilities.
Output: Working Next.js application with basic graph canvas that connects to backend data sources.
</objective>

<execution_context>
@/Users/briannielsen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/briannielsen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-graph-canvas/02-RESEARCH.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Next.js frontend with Cytoscape.js dependencies</name>
  <files>frontend/package.json, frontend/next.config.js, frontend/tsconfig.json, frontend/tailwind.config.js, frontend/src/app/layout.tsx</files>
  <action>
    Create Next.js 14 application in frontend/ directory with TypeScript and Tailwind CSS. Install Cytoscape.js 3.33.1, D3.js 7.9.0, and d3-zoom for graph visualization per research recommendations. Configure Next.js for client-side graph libraries (disable SSR for Cytoscape components). Setup Tailwind with canvas-optimized configurations for full-screen layouts. Use research-recommended dependencies exactly: cytoscape@3.33.1, d3@7.9.0, d3-zoom@latest. Include @types packages for TypeScript support.
  </action>
  <verify>cd frontend && npm install runs without errors, npm run dev starts development server successfully</verify>
  <done>Next.js frontend application compiles and serves with all graph visualization dependencies installed</done>
</task>

<task type="auto">
  <name>Task 2: Create Cytoscape.js graph canvas component</name>
  <files>frontend/src/components/GraphCanvas.tsx, frontend/src/app/page.tsx</files>
  <action>
    Implement GraphCanvas.tsx React component using Cytoscape.js with WebGL rendering for memory-safe performance per research patterns. Follow research Pattern 1 (progressive loading) and Pattern 2 (WebGL optimization) for 500+ node handling. Initialize with empty elements array, configure WebGL rendering, implement proper cleanup on component unmount to prevent memory leaks (critical per research Pitfall 1). Use d3-zoom for multi-device pan/zoom support per research recommendations. Add basic node and edge styles matching project relationship types (blocks, requires, feeds-into, tested-by, deployed-with). Integrate in page.tsx as main application interface with full-screen layout.
  </action>
  <verify>Canvas renders in browser at http://localhost:3000, pan/zoom responds to mouse and touch inputs, no console errors</verify>
  <done>Interactive graph canvas displays with working pan/zoom controls and memory-safe Cytoscape.js configuration</done>
</task>

<task type="auto">
  <name>Task 3: Integrate backend API and display work items</name>
  <files>frontend/src/lib/graphApi.ts, frontend/src/components/GraphCanvas.tsx</files>
  <action>
    Create graphApi.ts client for Phase 1 backend integration, implementing fetchWorkItems, createWorkItem, updateWorkItem functions that connect to Express API endpoints on port 3001. Add CORS-compliant fetch with proper error handling (400, 404, 500 status codes). Update GraphCanvas to load work items from API on mount and render as Cytoscape nodes with proper data mapping. Transform backend work item data to Cytoscape element format with node styling. Handle loading states and error scenarios gracefully. Follow established backend API contracts from 01-03 service layer summary.
  </action>
  <verify>Canvas displays work items from backend database, network tab shows successful API calls to localhost:3001</verify>
  <done>Graph canvas loads and displays actual work item data from Phase 1 backend with full API integration</done>
</task>

</tasks>

<verification>
- Frontend application starts successfully on http://localhost:3000
- Graph canvas component loads with Cytoscape.js WebGL rendering
- Pan and zoom interactions work on both mouse and touch devices
- Backend API integration displays work items as graph nodes
- Memory usage remains stable during canvas interactions
- No console errors or memory leaks detected
</verification>

<success_criteria>
- Next.js frontend serves interactive graph application
- Cytoscape.js renders work items with production-grade performance
- Canvas supports multi-device pan/zoom per GRAPH-02 requirement
- API integration connects frontend to Phase 1 backend data
- Memory-safe configuration handles large datasets per GRAPH-04 requirement
</success_criteria>

<output>
After completion, create `.planning/phases/02-graph-canvas/02-01-SUMMARY.md`
</output>