---
phase: 02-graph-canvas
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [
  "frontend/src/components/GraphCanvas.tsx",
  "frontend/src/components/NodeCreation.tsx",
  "frontend/src/components/RelationshipPanel.tsx",
  "frontend/src/lib/graphTypes.ts",
  "frontend/src/hooks/useGraphInteractions.tsx"
]
autonomous: false

must_haves:
  truths:
    - "User can create new nodes by clicking on canvas"
    - "User can select and manipulate existing nodes"
    - "User can create typed relationships between nodes"
    - "Relationship types display with distinct visual styling"
    - "NodeCreation and RelationshipPanel components share graph state"
  artifacts:
    - path: "frontend/src/components/NodeCreation.tsx"
      provides: "Interactive node creation interface"
      exports: ["NodeCreationPanel"]
    - path: "frontend/src/components/RelationshipPanel.tsx"
      provides: "Typed relationship creation UI"
      exports: ["RelationshipPanel"]
    - path: "frontend/src/lib/graphTypes.ts"
      provides: "TypeScript definitions for graph elements"
      exports: ["WorkItemNode", "RelationshipEdge", "RelationshipType"]
    - path: "frontend/src/hooks/useGraphInteractions.tsx"
      provides: "Custom hooks for graph interactions"
      exports: ["useNodeSelection", "useEdgeCreation"]
  key_links:
    - from: "frontend/src/components/NodeCreation.tsx"
      to: "backend API"
      via: "createWorkItem API call"
      pattern: "fetch.*api/work-items.*POST"
    - from: "frontend/src/components/RelationshipPanel.tsx"
      to: "Cytoscape.js"
      via: "edge creation and styling"
      pattern: "cy\\.add.*edge"
    - from: "frontend/src/hooks/useGraphInteractions.tsx"
      to: "GraphCanvas state"
      via: "React state management"
      pattern: "useState|useCallback"
---

<objective>
Implement interactive node manipulation and typed relationship creation, completing core graph visualization functionality.

Purpose: Enable users to create, select, and connect graph nodes with typed relationships, delivering GRAPH-01, GRAPH-03, and GRAPH-05 requirements with shared state management between components.
Output: Fully interactive graph canvas with node creation, selection, and relationship management capabilities.
</objective>

<execution_context>
@/Users/briannielsen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/briannielsen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-graph-canvas/02-RESEARCH.md
@.planning/phases/02-graph-canvas/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement interactive node creation and manipulation</name>
  <files>frontend/src/components/NodeCreation.tsx, frontend/src/lib/graphTypes.ts, frontend/src/hooks/useGraphInteractions.tsx</files>
  <action>
    Create NodeCreation.tsx component with form for work item creation (name, description, type fields). Implement graphTypes.ts with TypeScript interfaces for WorkItemNode, RelationshipEdge, and RelationshipType matching backend data models. Add useGraphInteractions.tsx hook with useNodeSelection for node selection state management and click handlers. Integrate node creation with backend API calls through graphApi client. Handle node positioning on canvas click, validate form data, and provide user feedback for creation success/failure. Use proper React patterns with useCallback for performance optimization.
  </action>
  <verify>User can click empty canvas area to create new nodes, form appears with proper validation, new nodes appear on canvas</verify>
  <done>Interactive node creation works with backend integration and proper form validation</done>
</task>

<task type="auto">
  <name>Task 2: Add typed relationship creation with visual styling and component integration</name>
  <files>frontend/src/components/RelationshipPanel.tsx, frontend/src/components/GraphCanvas.tsx</files>
  <action>
    Implement RelationshipPanel.tsx with relationship type selector (blocks, requires, feeds-into, tested-by, deployed-with) and source/target node selection interface. Update GraphCanvas.tsx to support edge creation mode: users select source node, choose relationship type, then select target node to create connection. Apply research Pattern 3 (typed relationship styling) with distinct colors and arrow shapes for each relationship type: blocks=red/triangle, requires=blue/diamond, feeds-into=green/circle, tested-by=orange/square, deployed-with=purple/vee. Implement edge creation through Cytoscape.js add() method with proper data structure. Ensure NodeCreation and RelationshipPanel components share graph state through useGraphInteractions hook - both components access same selected nodes, creation modes, and update handlers for seamless integration.
  </action>
  <verify>Users can create relationships between nodes, each relationship type displays with unique styling, relationships persist in backend, components share state properly</verify>
  <done>Typed relationship creation works with distinct visual styling, backend persistence, and integrated component state sharing</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Interactive graph canvas with node creation, relationship management, and integrated component state sharing</what-built>
  <how-to-verify>
    1. Navigate to http://localhost:3000
    2. Test node creation: click empty canvas, fill form, verify node appears
    3. Test node selection: click existing nodes, verify selection highlighting
    4. Test relationship creation: select two nodes, choose relationship type, verify edge appears with correct styling
    5. Test component integration: verify NodeCreation and RelationshipPanel components work together seamlessly
    6. Test multi-device: use touch gestures on mobile/tablet, verify pan/zoom works
    7. Verify backend persistence: refresh page, confirm nodes and relationships persist
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- Node creation interface allows work item creation with form validation
- Graph supports interactive node selection and manipulation
- Typed relationship creation works with distinct visual styling per type
- NodeCreation and RelationshipPanel components share graph state properly
- Multi-device pan/zoom interactions work smoothly on touch and mouse
- All interactions persist to backend and survive page refresh
</verification>

<success_criteria>
- User can create interactive nodes on canvas (GRAPH-01 requirement met)
- Canvas supports pan and zoom on any device (GRAPH-02 requirement met)
- User can select and manipulate nodes with touch/mouse (GRAPH-03 requirement met)
- User can create typed relationships between nodes (GRAPH-05 requirement met)
- Components integrate seamlessly with shared state management
- Core graph visualization functionality completed
</success_criteria>

<output>
After completion, create `.planning/phases/02-graph-canvas/02-02-SUMMARY.md`
</output>